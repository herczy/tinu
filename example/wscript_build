task = bld.new_task_gen('cc', 'cprogram')
task.target = 'example'
task.includes = '. # ../lib ../lib'
task.source = 'example.c'
task.install_path = '${PREFIX}/share/tinu'
task.add_objects = 'static_tinu'
task.uselib = 'GLIB GTHREAD DL ELF DWARF'

# vim: syntax=python
